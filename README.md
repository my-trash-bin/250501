# Setup guide

## Initial Project Setup

```sh
mise use node
npm i -g yarn
npx yarn set version stable
yarn config set nodeLinker pnp
# TODO: add project
npx yarn dlx @yarnpkg/sdks vscode
```

## Setup

```sh
mise install
npm i -g yarn
npx yarn set version stable
npx yarn dlx @yarnpkg/sdks vscode
# vscode command palette: `>TypeScript: Select Typescript Version...`
```
